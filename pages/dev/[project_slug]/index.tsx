import ProjectSlug, {
  getStaticPaths as gsPaths,
  getStaticProps as gsProps,
} from "@pageComponents/ProjectSlug";

export default ProjectSlug;
export const getStaticPaths = gsPaths;
export const getStaticProps = gsProps;
